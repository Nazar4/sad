export class Television {
    id: number;
    packageType: string;
    description?: string;
    price: number;

    // get fullName() {
    //     return `${this.firstName} ${this.lastName}`
    // }
}